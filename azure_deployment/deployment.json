{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.38.33.27573",
      "templateHash": "5517853380781368327"
    }
  },
  "parameters": {
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]",
      "metadata": {
        "description": "Location for all resources"
      }
    },
    "containerGroupName": {
      "type": "string",
      "metadata": {
        "description": "Name of the Azure Container Group"
      }
    },
    "containerVersion": {
      "type": "string",
      "defaultValue": "1",
      "metadata": {
        "description": "Container image version to deploy"
      }
    },
    "requireTls": {
      "type": "bool",
      "defaultValue": true,
      "metadata": {
        "description": "Require TLS for SMTP connections"
      }
    },
    "envVars": {
      "type": "object",
      "defaultValue": {},
      "metadata": {
        "description": "SMTP OAuth Relay environment variables"
      }
    },
    "lookupTableUrl": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "URL of the Azure Table to use for lookup"
      }
    },
    "tlsCertKeyVaultUrl": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Key Vault URL where the TLS certificate is stored"
      }
    },
    "tlsCertName": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Name of the TLS certificate to use"
      }
    }
  },
  "variables": {
    "containerImage": "[format('ghcr.io/justiniven/smtp-oauth-relay:{0}', parameters('containerVersion'))]",
    "finalEnvVars": "[shallowMerge(createArray(createObject('REQUIRE_TLS', string(parameters('requireTls'))), if(parameters('requireTls'), createObject('TLS_SOURCE', if(and(not(equals(parameters('tlsCertKeyVaultUrl'), '')), not(equals(parameters('tlsCertName'), ''))), 'keyvault', 'file')), createObject()), if(not(equals(parameters('lookupTableUrl'), '')), createObject('AZURE_TABLES_URL', parameters('lookupTableUrl')), createObject()), if(not(equals(parameters('tlsCertKeyVaultUrl'), '')), createObject('AZURE_KEY_VAULT_URL', parameters('tlsCertKeyVaultUrl')), createObject()), if(not(equals(parameters('tlsCertName'), '')), createObject('AZURE_KEY_VAULT_CERT_NAME', parameters('tlsCertName')), createObject()), parameters('envVars')))]"
  },
  "resources": [
    {
      "type": "Microsoft.ContainerInstance/containerGroups",
      "apiVersion": "2023-05-01",
      "name": "[parameters('containerGroupName')]",
      "location": "[parameters('location')]",
      "identity": {
        "type": "SystemAssigned"
      },
      "properties": {
        "containers": [
          {
            "name": "smtp-oauth-relay",
            "properties": {
              "copy": [
                {
                  "name": "environmentVariables",
                  "count": "[length(objectKeys(variables('finalEnvVars')))]",
                  "input": {
                    "name": "[objectKeys(variables('finalEnvVars'))[copyIndex('environmentVariables')]]",
                    "value": "[variables('finalEnvVars')[objectKeys(variables('finalEnvVars'))[copyIndex('environmentVariables')]]]"
                  }
                }
              ],
              "image": "[variables('containerImage')]",
              "resources": {
                "requests": {
                  "cpu": 1,
                  "memoryInGB": 2
                }
              },
              "ports": [
                {
                  "protocol": "TCP",
                  "port": 8025
                }
              ]
            }
          }
        ],
        "restartPolicy": "OnFailure",
        "osType": "Linux",
        "ipAddress": {
          "type": "Public",
          "autoGeneratedDomainNameLabelScope": "SubscriptionReuse",
          "dnsNameLabel": "[toLower(parameters('containerGroupName'))]",
          "ports": [
            {
              "protocol": "TCP",
              "port": 8025
            }
          ]
        }
      }
    }
  ]
}